rules:
  default:
    protection:
      required_status_checks:
        strict: true
        contexts:
          - continuous-integration/travis-ci/pr
          - continuous-integration/appveyor/pr
          - codecov/project
          - pyup.io/safety-ci
